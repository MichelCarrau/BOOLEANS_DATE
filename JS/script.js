let numOne = 8
let numTwo = 4
let suma = numOne + numTwo
let resta = numOne + numTwo
let multiplicacion = numOne * numTwo
let division = numOne / numTwo
let porcentaje = numOne % numTwo
let exponente = numOne ** numTwo

console.log(suma, resta, multiplicacion, division, porcentaje, exponente)
const PI = 3.14
let radius = 100

//area del circulo
const areaOfCircle = PI * radius * radius
console.log(areaOfCircle)

//Peso de un objeto
const gravity = 9.81
let mass = 72

const weigth = mass * gravity
console.log(weigth)

const boillingPoint = 100; // Punto de ebullición del agua en Celsius
const bodyTemp = 37;       // Temperatura del cuerpo humano en Celsius

console.log(`El punto de ebullición del agua es de ${boillingPoint} grados Celsius\nLa temperatura del cuerpo humano es de ${bodyTemp} grados Celsius\nLa gravedad de la Tierra es ${gravity} m/s2`);
